# CodexChat 伴生会话管理器需求文档

## 1. 文档信息

- 产品名称：CodexChat
- 产品定位：Codex 本地会话伴生管理器
- 目标平台：Visual Studio Code
- 当前版本：需求分析版
- 文档状态：待评审
- 编写日期：2026-08-19

## 2. 背景与问题

当前 Codex 在不同项目中产生的对话统一存储在本机 Codex 数据目录中。随着项目数量和对话数量增加，所有对话混在一起，用户难以快速判断某个会话属于哪个项目，也难以按项目回顾历史工作。

用户希望参考 Codex Desktop 的项目级管理方式，在 VS Code 中增加一个伴生管理器：

1. 按项目文件夹组织 Codex 会话。
2. 显示项目名称、项目路径和会话创建/更新时间。
3. 用户进入管理器后先选择项目文件夹。
4. 选择项目后，仅加载该项目对应的会话。
5. 读取已有 Codex 本地会话，不重新实现聊天功能。

## 3. 产品定位与边界

### 3.1 产品定位

CodexChat 是一个“本地会话浏览与项目分类管理器”，作为现有 Codex VS Code 扩展或 Codex CLI 的伴生工具使用。

### 3.2 本期包含

- 读取本机 Codex 会话目录。
- 自动识别会话所属项目目录。
- 按项目建立会话分组。
- 进入插件后选择或切换项目。
- 查看项目下的会话列表。
- 查看会话内容。
- 按创建时间或更新时间排序。
- 监听会话目录变化并刷新索引。
- 在本地保存管理器自身的项目选择和索引缓存。

### 3.3 本期不包含

- 不实现新的聊天窗口。
- 不调用 OpenAI API 创建独立对话。
- 不替代现有 Codex VS Code 扩展。
- 不修改 Codex 原始会话文件。
- 不默认删除、移动或重写 Codex 会话。
- 不依赖官方 Codex 扩展的未公开内部接口。

### 3.4 后续可选能力

- 通过 Codex CLI 恢复指定会话。
- 搜索全部项目或当前项目的会话内容。
- 会话归档、标记、收藏和备注。
- 导出为 Markdown、HTML 或 JSON。
- 识别项目目录改名或移动后的关联关系。
- 在官方插件开放稳定命令后，跳转到指定会话。

## 4. 数据来源

### 4.1 默认数据目录

Windows 默认读取：

```text
C:\Users\Administrator\.codex
```

实际实现不应把用户名写死，应该优先支持以下配置方式：

1. 用户设置的 Codex 数据目录。
2. 系统用户目录下的 `.codex` 目录。
3. 如果目录不存在，显示配置提示，而不是静默失败。

### 4.2 已发现的目录结构

```text
.codex/
├─ sessions/
│  └─ YYYY/MM/DD/rollout-*.jsonl
├─ archived_sessions/
│  └─ rollout-*.jsonl
├─ session_index.jsonl
├─ history.jsonl
└─ 其他 Codex 状态、日志和数据库文件
```

### 4.3 会话文件

会话文件是 JSON Lines 格式，每行是一个 JSON 对象。第一条记录通常为 `session_meta`，包含会话的基础信息：

- `session_id`：会话 ID。
- `id`：会话 ID 或关联 ID。
- `timestamp`：会话创建时间。
- `cwd`：会话启动时的工作目录，即项目目录的主要识别依据。
- `originator`：会话来源。
- `cli_version`：创建会话时的 Codex 版本。
- `source`、`thread_source`：会话来源信息。

后续记录可能包含用户消息、助手消息、工具调用、事件、摘要、上下文和加密内容。读取器必须按记录类型容错解析，不得假定所有记录都有相同字段。

### 4.4 索引文件

`session_index.jsonl` 可用于快速获取部分会话的：

- 会话 ID。
- 会话标题。
- 更新时间。

该文件只能作为加速索引，不能作为唯一数据来源。原因是索引内容可能与实际会话文件不同步，也可能不包含全部会话。因此最终索引应以 `sessions` 和 `archived_sessions` 中的实际会话文件为准。

## 5. 用户角色与使用场景

### 5.1 目标用户

在 VS Code 中使用 Codex，并同时维护多个代码项目的个人开发者。

### 5.2 典型场景

#### 场景 A：首次进入

用户打开 VS Code 的 CodexChat 视图。插件读取本地会话，识别出多个项目，展示项目选择页。用户选择项目文件夹后，进入该项目的会话列表。

#### 场景 B：项目切换

用户当前正在查看项目 A，希望查看项目 B 的历史会话。用户点击“切换项目”，选择项目 B，管理器刷新并仅展示项目 B 的会话。

#### 场景 C：查看历史对话

用户按更新时间找到某个会话，打开后查看用户消息、助手回复和关键工具操作记录。

#### 场景 D：新会话产生

用户通过现有 Codex 扩展或 CLI 创建新会话。CodexChat 监听目录变化，自动发现新会话，并将其归入对应项目。

#### 场景 E：没有会话的项目

用户选择了一个尚未使用 Codex 的项目文件夹。管理器显示空状态，并明确说明该项目目前没有发现本地 Codex 会话。

## 6. 核心用户流程

```text
启动 VS Code
    ↓
打开 CodexChat
    ↓
读取 Codex 数据目录
    ↓
扫描会话文件并提取 cwd
    ↓
生成项目列表
    ↓
用户选择项目文件夹
    ↓
加载该项目的会话列表
    ↓
用户选择会话
    ↓
读取并展示会话内容
```

### 6.1 首次进入规则

- 如果没有配置 Codex 数据目录，提示用户配置目录。
- 如果目录存在但没有会话，显示空状态。
- 如果只有一个项目，可自动进入该项目，但仍提供切换项目入口。
- 如果有多个项目，优先显示项目选择页。
- 如果用户之前选择过项目且该目录仍存在，可恢复上次项目。
- 如果上次项目已不存在，回到项目选择页。

### 6.2 项目选择规则

项目选择页应同时支持：

- 从自动识别的项目列表中选择。
- 使用系统文件夹选择器选择其他目录。
- 按项目名称排序。
- 按最近会话时间排序。
- 显示项目名称和完整路径，避免同名项目混淆。

用户手动选择一个目录后，如果该目录已有对应会话，应立即加载；如果没有对应会话，可以进入该项目的空状态，并允许后续被新会话自动识别。

### 6.3 会话列表规则

默认按最近更新时间倒序排列。每个会话至少显示：

- 会话标题。
- 最近更新时间。
- 创建时间或相对时间。
- 会话状态：正常或已归档。
- 可选的会话 ID 简短标识。

标题优先级建议为：

1. `session_index.jsonl` 中的 `thread_name`。
2. 会话中第一条有效用户消息的摘要。
3. 会话文件名。
4. 会话 ID。

### 6.4 会话详情规则

会话详情以只读方式展示，至少支持：

- 用户消息。
- 助手文本回复。
- 工具调用摘要。
- 时间信息。
- 会话所属项目路径。
- 会话 ID。
- 打开原始文件所在目录。

无法识别的记录类型不能导致整个会话加载失败，应以折叠的“未识别事件”或简略记录展示，或跳过并记录诊断信息。

## 7. 功能需求

### FR-01：读取 Codex 数据目录

插件应读取用户配置的 Codex 数据目录。默认目录为当前系统用户目录下的 `.codex`。

验收标准：

- 能检测目录是否存在。
- 能处理目录不可访问的情况。
- 能在设置中修改目录。
- 修改后可以重新扫描。

### FR-02：扫描会话文件

插件应扫描：

- `.codex/sessions/**/*.jsonl`
- `.codex/archived_sessions/*.jsonl`

扫描过程应支持大文件、空文件、部分写入文件和单行格式错误。

验收标准：

- 单个损坏文件不会阻塞其他会话加载。
- 能根据文件变化增量更新。
- 首次扫描完成前显示加载状态。

### FR-03：识别项目

插件应从会话的 `session_meta.payload.cwd` 获取项目目录，并对路径进行规范化。

路径处理至少包括：

- Windows 大小写差异。
- `\\` 和 `/` 分隔符差异。
- 末尾分隔符差异。
- 相对路径或异常路径的容错。
- 无 `cwd` 会话的“未归类”分组。

### FR-04：项目列表

项目列表应显示：

- 项目名称。
- 完整路径。
- 会话数量。
- 最近会话时间。
- 项目首次发现时间。
- 是否为当前项目。

项目首次发现时间定义为管理器第一次发现该项目会话的时间，而不是项目文件系统创建时间。

### FR-05：选择和切换项目

插件启动后应允许用户选择项目。项目切换后，当前视图、会话数量和会话详情都必须更新为新项目的数据。

### FR-06：查看会话

用户可以打开会话并查看内容。查看操作不得修改源 JSONL 文件。

### FR-07：刷新

提供手动刷新命令，并支持文件系统监听自动刷新。自动刷新需要防抖，避免 Codex 连续写入时频繁重建整个界面。

### FR-08：会话定位

提供以下只读辅助操作：

- 复制会话 ID。
- 在文件管理器中打开原始会话文件所在目录。
- 打开项目目录。

通过 Codex CLI 恢复会话属于后续能力，不列入第一版强制验收范围。

### FR-09：搜索和排序

第一版至少支持按更新时间排序。搜索可以作为 MVP 后续的小版本能力，搜索范围包括会话标题和首条用户消息。

### FR-10：诊断信息

插件应提供诊断入口，显示：

- 当前 Codex 数据目录。
- 最近扫描时间。
- 扫描到的会话数量。
- 识别到的项目数量。
- 解析失败文件数量。
- 最近一次错误原因。

## 8. 数据模型

### 8.1 项目模型

```json
{
  "id": "normalized-project-path-hash",
  "name": "项目名称",
  "path": "D:\\项目\\Example",
  "firstDiscoveredAt": "2026-08-19T06:37:32.975Z",
  "lastOpenedAt": "2026-08-19T06:45:00.000Z",
  "conversationCount": 12,
  "lastConversationAt": "2026-08-19T06:40:00.000Z"
}
```

项目 ID 应由规范化后的绝对路径生成，避免同一路径因大小写或分隔符差异产生重复项目。

### 8.2 会话索引模型

```json
{
  "id": "01a018bb-3ae3-7740-adb7-cc355180f5f5",
  "projectPath": "D:\\项目\\Example",
  "filePath": "C:\\Users\\Administrator\\.codex\\sessions\\2026\\08\\19\\rollout-....jsonl",
  "title": "会话标题",
  "createdAt": "2026-08-19T06:37:32.975Z",
  "updatedAt": "2026-08-19T06:40:00.000Z",
  "archived": false,
  "fileSize": 123456,
  "fileModifiedAt": "2026-08-19T06:40:01.000Z"
}
```

### 8.3 消息展示模型

内部展示模型不应直接绑定完整的 Codex 私有记录结构，建议抽象为：

```json
{
  "kind": "user | assistant | tool | event | unknown",
  "content": "展示内容",
  "createdAt": "2026-08-19T06:40:00.000Z",
  "collapsed": false,
  "sourceType": "response_item"
}
```

这样可以在 Codex 会话格式变化时，尽量保持界面层稳定。

## 9. 存储与缓存

### 9.1 原则

- Codex 原始数据只读。
- 不把管理器索引写入用户项目目录。
- 管理器自身配置和缓存存放在 VS Code 的扩展存储目录。
- 缓存可以删除并从源文件重新生成。

### 9.2 建议缓存内容

- 用户配置的 Codex 数据目录。
- 最近选择的项目路径。
- 项目元数据。
- 会话索引的文件路径、文件大小和修改时间。
- 解析结果或摘要缓存。
- 解析错误记录。

缓存必须以文件路径和修改时间为校验依据。源文件发生变化时，应重新读取对应会话。

## 10. 界面需求

### 10.1 VS Code 集成

建议通过以下方式集成：

- Activity Bar 中提供 CodexChat 图标。
- 侧边栏显示项目和会话管理视图。
- 命令面板提供项目切换、刷新和设置入口。
- 使用 VS Code 原生主题颜色，适配浅色、深色和高对比度主题。

### 10.2 页面状态

至少需要以下状态：

- 未配置数据目录。
- 数据目录不存在。
- 首次扫描中。
- 未选择项目。
- 已选择项目但无会话。
- 项目有会话但详情未打开。
- 正在加载会话详情。
- 会话内容加载完成。
- 会话文件部分损坏。
- 目录无访问权限。

### 10.3 空状态文案方向

文案应直接说明当前状态和可执行操作，例如：

- “尚未配置 Codex 数据目录”
- “未发现本地 Codex 会话”
- “当前项目暂无会话”
- “部分会话无法解析，可查看诊断信息”

## 11. 兼容性与稳定性要求

### 11.1 私有格式风险

Codex 的 JSONL 会话格式属于本地实现细节，不能视为长期稳定的公开接口。因此读取器必须：

- 只依赖必要字段，如会话元数据中的 `cwd`、会话 ID 和时间。
- 对未知记录类型保持兼容。
- 对缺失字段提供默认值。
- 记录 Codex 版本和格式异常。
- 不直接修改源文件。

### 11.2 并发写入

Codex 可能正在写入会话文件。读取时可能遇到最后一行未完成或文件暂时被占用。

处理要求：

- JSONL 按行读取。
- 最后一行解析失败时允许等待后重试。
- 已完成的前序记录仍应展示。
- 不因尾部半行导致整个会话不可见。

### 11.3 大会话性能

对于较大的会话文件：

- 首次列表加载只读取元数据，不读取完整正文。
- 打开详情时再读取正文。
- 详情展示应支持分段或分页加载。
- 避免一次性把所有项目的完整对话读入内存。
- 文件监听只重新处理变化的文件。

### 11.4 路径变化

会话记录中的 `cwd` 可能指向已经不存在、被改名或被移动的目录。此类项目仍应保留在“历史项目”中，并标记路径不可用，不应直接丢弃会话。

## 12. 安全与隐私要求

Codex 会话可能包含源代码、密钥片段、业务数据和个人信息。插件必须遵守以下原则：

- 默认只访问用户配置的 `.codex` 目录。
- 默认只读，不上传会话内容。
- 不将会话内容写入日志。
- 诊断日志中隐藏消息正文、令牌和路径中的敏感信息。
- 导出、复制等操作必须由用户主动触发。
- 不扫描与功能无关的目录。
- 明确告知用户会话内容来自本机本地文件。

## 13. MVP 范围

第一版建议包含：

1. VS Code Activity Bar 入口。
2. Codex 数据目录检测和设置。
3. 扫描 `sessions` 和 `archived_sessions`。
4. 从 `cwd` 自动识别项目。
5. 项目选择和切换。
6. 当前项目的会话列表。
7. 会话标题、创建时间、更新时间和归档状态。
8. 只读会话详情查看。
9. 手动刷新和基本文件监听。
10. 复制会话 ID和打开原始文件目录。
11. 基本错误提示和诊断信息。

第一版不要求实现聊天、编辑历史、删除源文件或直接控制官方 Codex 扩展。

## 14. 后续版本规划

### V1.1：检索增强

- 当前项目内搜索。
- 全项目搜索。
- 按日期筛选。
- 按标题、更新时间和创建时间排序。
- 会话收藏和备注。

### V1.2：会话操作

- 归档视图。
- 导出 Markdown。
- 复制用户问题或助手回答。
- 通过 Codex CLI 恢复会话。

### V2.0：深度集成

- 与官方 Codex 扩展公开命令集成。
- 从管理器打开指定会话。
- 项目工作区与 Codex 会话联动。
- 可选的跨设备同步，但必须明确隐私边界。

## 15. 验收标准

### 项目识别

- 插件能够从本地会话中识别项目路径。
- 同一个项目不会因路径大小写或分隔符不同重复显示。
- 没有 `cwd` 的会话会进入未归类分组。

### 项目选择

- 用户首次进入时可以选择项目文件夹。
- 选择项目后只显示该项目对应的会话。
- 可以在多个项目之间切换。
- 关闭并重新打开 VS Code 后能够恢复上次项目。

### 会话展示

- 能展示会话标题、创建时间和更新时间。
- 能打开并查看用户消息和助手文本。
- 会话文件包含未知事件时仍能展示可识别内容。
- 正在写入或尾部损坏的文件不会导致插件整体崩溃。

### 实时刷新

- 新建会话后，经过短暂刷新延迟能够出现在对应项目中。
- 会话继续追加内容时，列表更新时间能够更新。
- 文件删除或移动后，管理器显示合理状态。

### 安全性

- 不修改原始 JSONL 会话。
- 不上传本地会话内容。
- 日志不包含完整会话正文。

## 16. 未决问题

以下问题在进入开发前需要确认：

1. 是否只支持 Windows，还是同时支持 macOS 和 Linux。
2. Codex 数据目录是否允许通过 VS Code 设置手动指定。
3. 是否需要读取归档会话，还是默认隐藏并单独显示。
4. 会话详情是否需要完整展示工具调用和代码变更。
5. 是否需要兼容旧版本 Codex 的会话格式。
6. 是否需要提供 Codex CLI 恢复入口。
7. 项目文件夹改名或移动后，是否允许用户手动合并项目。
8. 是否需要全局搜索，以及大规模会话下的索引策略。

## 17. 关键结论

CodexChat 不应成为新的聊天客户端，而应专注于解决“本地 Codex 会话按项目归类和查找”的问题。

实现上以 `C:\Users\Administrator\.codex` 为默认数据源，以会话文件中的 `cwd` 作为项目归属依据，以 JSONL 文件扫描为事实来源，以 `session_index.jsonl` 作为可选的加速索引，并始终保持对 Codex 原始文件的只读访问。

第一版先完成“项目选择、会话列表、会话查看”这条主流程，再根据实际会话格式和使用反馈扩展搜索、归档、导出以及 CLI 恢复能力。
